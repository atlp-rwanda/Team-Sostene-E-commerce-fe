import { ErrorBox } from '../Reusable'; // Adjust the import path according to your file structure

export default {
  title: 'Components/Reusables/ErrorBox',
  component: ErrorBox,
};

export const Default = {
  args: {
    message: 'An error occurred.',
  },
};
